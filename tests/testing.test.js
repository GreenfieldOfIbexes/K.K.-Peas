import makeProductObj from "../src/actions/makeProductObj-possibleRefile.js";
const util = require("util");
test("", async () => {
	// let product = await makeProductObj(2);
	// console.log(util.inspect(product, { depth: null }));
});
